# Conversion Component

The conversion component owns the `convert-length` command. It converts values by normalizing the input unit to meters and then converting the normalized value to the requested target unit.

This component is intentionally removable on a branch so ingestion can observe feature deletion through removed files.
