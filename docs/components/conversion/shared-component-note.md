# Shared Canonical Fixture

This file is intentionally byte-for-byte identical in multiple component paths.

Use it to validate canonical document grouping across app, component, and test-data locations.
