# Calculator Component

The calculator component owns the arithmetic commands in the CLI. Each command accepts two numeric operands and returns a single numeric result.

Core commands on the stable `main` branch are `add`, `sub`, `mul`, and `div`.
